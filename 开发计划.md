# 开发计划：转转点餐 V1.0（MVP）

> 依据《MRD：福利点餐小工具》（见 [MRD.md](MRD.md)）制定。本期目标：以最小可用产品验证「随机转盘 → 附近可下单店铺 → 一键下单」核心闭环。

---

## 一、目标功能

### 1.1 范围内功能（P0）

| # | 功能 | 说明 | 验收标准 |
|---|---|---|---|
| F1 | 定位与常用地址 | H5 Geolocation + 预设办公地址（国贸CBD / 望京SOHO / 中关村）手动回退 | 进入首页后自动确定可用地址，可随时切换 |
| F2 | 随机转盘 | Canvas 转盘，6–8 个扇区 = 加权随机抽取的"当前可下单"店铺；候选不足 6 家自动降级为品类转盘 | 点击转动 3–5 秒出结果，带减速动画与音效；同会话重转不重复 |
| F3 | 转盘结果卡片 | 店铺评分 / 人均 / 距离 / 配送费 / 送达时间；操作：去下单、排除再转、换一批、分享卡片、拉黑 | 排除上限 5 家；「去下单」直达该店，所见即所得 |
| F4 | 附近店铺发现 | 3km 内可下单店铺列表：品类筛选、距离 / 评分 / 人均排序、营业状态与「即将打烊」标注 | 与转盘共用同一套"可下单"判定逻辑 |
| F5 | 店铺详情与下单 | 菜单浏览、购物车、提交订单（模拟支付），服务端校验起送价与价格 | 转盘结果与下单店铺强制一致 |
| F6 | 订单跟踪 | 订单列表 + 状态时间线（已下单 → 商家接单 → 配送中 → 已送达，按时间自动推进） | 状态无需刷新页面即自动更新 |
| F7 | 偏好与黑名单 | 人均上限、忌口标签过滤转盘候选池；黑名单店铺全局排除 | 偏好修改即时生效并持久化 |
| F8 | 业务规则落地 | 加权随机（评分 ≥ 4.5 / 新店加权）、24h 店铺、即将打烊（< 30min）、排除上限、空态引导、结果分享 | 逐条对应 MRD §5 |

### 1.2 范围外（后续迭代）
真实外卖平台跳转 / CPS 分佣、真实支付、地图 SDK 与真实 POI 数据、团队拼单转盘、企业福利（B 端）、消息推送。

---

## 二、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML5 + CSS3 + JavaScript（ES Modules），Canvas 绘制转盘 | 零构建、零依赖，浏览器直接运行；MVP 以交互验证为先 |
| 服务端 | Node.js 原生 `http` 模块（无框架、无 npm 依赖） | `node server/server.js` 一键启动、环境可复现；路由 / 服务分层清晰，可平滑迁移 Express/Nest |
| 数据层 | JSON 模拟数据（3 商圈 × 27 家店铺 + 菜单）+ 内存订单表 | 无需数据库即可演示；数据访问收敛在 service 层，替换数据源即接真实 API |
| 定位 | H5 Geolocation + 预设地址回退 | 预留高德 / 腾讯地图 Web API 接入位 |
| 运行环境 | Node.js ≥ 16；Chrome / Safari / Edge 现代浏览器 | 本机 Node v24 ✓ |

**架构原则**：前端 `api.js` 统一封装接口、视图（views）与组件分离；服务端 routes / services / data 三层分离，保证"演示版 → 真实版"替换成本最低。

---

## 三、模块划分

### 3.1 系统架构

```
┌─────────────────── 浏览器（H5，移动端优先）───────────────────┐
│  views: home(转盘) / shops(附近店铺) / shop(下单) / orders / me │
│  组件: wheel.js(Canvas转盘)  ui.js(弹层/音效)  topbar.js(地址)  │
│  状态: state.js(偏好/购物车/会话排除)  geo.js(定位)  api.js     │
└──────────────────────────────┬───────────────────────────────┘
                        REST JSON│ fetch
┌──────────────────────────────▼───────────────────────────────┐
│  server/server.js（路由分发 / 静态资源，零依赖）               │
│   ├ services/shopService.js   店铺筛选 · 距离 · 营业状态       │
│   ├ services/wheelService.js  候选池 · 加权随机 · 品类降级     │
│   ├ services/orderService.js  下单校验 · 订单状态时间线        │
│   └ lib/geo.js                Haversine 距离 · 营业时间判断    │
│         data/shops.json（演示数据）      内存订单表             │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 服务端模块
| 模块 | 职责 |
|---|---|
| `server/server.js` | HTTP 服务、静态资源、API 路由分发、JSON 解析与错误处理 |
| `server/services/shopService.js` | 店铺库加载、Haversine 距离、营业状态（营业中/即将打烊/休息）、筛选排序 |
| `server/services/wheelService.js` | 转盘候选池（可下单 + 偏好 + 排除过滤）、加权随机采样、品类转盘降级 |
| `server/services/orderService.js` | 下单校验（起送价、菜单价以服务端为准）、订单创建、状态时间线模拟 |
| `server/lib/geo.js` | Haversine 距离、营业时间 / 打烊倒计时计算 |
| `server/data/shops.json` | 演示数据：3 商圈 27 店、约 135 道菜、地址预设、忌口标签 |

### 3.3 前端模块
| 模块 | 职责 |
|---|---|
| `web/js/app.js` | 启动流程（加载元数据 → 定位解析 → 渲染）、hash 路由 |
| `web/js/topbar.js` | 顶栏地址切换（定位按钮 + 预设地址弹层） |
| `web/js/api.js` | REST 接口统一封装与错误归一 |
| `web/js/state.js` | 偏好 / 常用地址（localStorage 持久化）、购物车、会话级排除列表 |
| `web/js/geo.js` | 浏览器定位（超时与拒绝回退） |
| `web/js/ui.js` | 底部弹层、Toast、WebAudio 音效、格式化工具 |
| `web/js/wheel.js` | Canvas 转盘组件：扇区绘制、缓动动画、落点计算、转动音效 |
| `web/js/views/home.js` | 转盘页：加载候选 → 转动 → 结果卡片 → 排除 / 换一批 / 分享 / 下单 |
| `web/js/views/shops.js` | 附近店铺列表：筛选、排序、营业状态 |
| `web/js/views/shop.js` | 店铺详情、菜单、购物车、提交订单 |
| `web/js/views/orders.js` | 订单列表与状态自动推进 |
| `web/js/views/me.js` | 常用地址、偏好、黑名单管理 |

### 3.4 API 契约
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/meta` | 地址预设、品类清单、忌口标签 |
| GET | `/api/shops` | 附近店铺（lat,lng,category,sort,openOnly,maxPrice） |
| GET | `/api/shops/:id` | 店铺详情 + 菜单（带距离与营业状态） |
| POST | `/api/wheel` | 转盘候选生成（加权随机 / 排除 / 偏好 / 品类降级） |
| POST | `/api/orders` | 创建订单（校验起送价与菜单价格） |
| GET | `/api/orders` | 订单列表（含状态时间线） |

### 3.5 数据模型（核心字段）
- **Shop**：id / name / short（转盘短名）/ emoji / cuisine / rating / monthlySales / avgPrice / lat / lng / zone / openTime / closeTime / deliveryFee / minOrder / deliveryMinutes / isNew / license / tags（忌口相关）/ dishes[]
- **Dish**：id / name / price / desc / isSignature
- **Order**：id / shopId / items[] / subtotal / deliveryFee / total / fromWheel / addressName / remark / createdAt / timeline{accepted, delivering, delivered}

---

## 四、开发步骤与时间安排

人力基准：1 名全栈开发，共 **5 个工作日**；设 M1–M4 四个里程碑。

| 阶段 | 时间 | 任务 | 产出 / 里程碑 |
|---|---|---|---|
| S1 数据层 | D1 上午 | 项目脚手架、演示数据（27 店 + 菜单 + 地址预设）、geo 工具库 | 数据层完成 |
| S2 服务端 | D1 下午 – D2 | shop / wheel / order 三大服务 + API 路由；curl 接口自测（含异常用例） | **M1：6 个 API 全部可用** |
| S3 前端骨架与转盘 | D3 | 布局 / 路由 / 定位 / 状态 / 弹层等基础设施；Canvas 转盘（动画、音效、落点） | **M2：转盘可转出结果** |
| S4 店铺与下单 | D4 | 附近店铺列表筛选、店铺详情、购物车、提交订单 | **M3：转盘→下单全流程打通** |
| S5 打磨与验收 | D5 | 订单跟踪、偏好 / 黑名单、规则打磨（排除上限 / 即将打烊 / 空态 / 分享卡片）、端到端测试、README | **M4：验收通过** |

并行说明：S2（后端）与 S3 骨架（前端）可并行；单人串行时按表执行。本次开发按同一阶段顺序实施，进度见第七节勾选。

---

## 五、测试与验收计划
- **接口测试**：curl 覆盖 6 个 API + 异常用例（起送价不足、店铺不存在、坐标无覆盖）。
- **业务规则用例**：转盘重转不重复、排除 5 家达上限、偏好过滤生效、品类转盘降级、起送价校验、订单状态按时间推进、即将打烊标注。
- **端到端**：浏览器走查「定位 → 转盘 → 结果 → 下单 → 订单」全流程 + 移动端视口核对。
- **验收口径**：以 MRD §5 业务规则逐条核对。

## 六、风险与应对
| 风险 | 应对 |
|---|---|
| Geolocation 需 HTTPS/localhost 且可能被拒绝 | localhost 开发天然满足；预设地址一键回退 |
| 真实定位远离演示数据覆盖区出现空态 | 空态卡片引导一键切换演示商圈 |
| 无真实店铺 / 交易数据 | 数据源收敛在 service 层，替换即接真实 API |
| 浏览器自动播放限制导致音效失效 | 音效在用户点击手势后初始化，失败静默降级 |

## 七、执行进度（开发时同步更新）
- [x] S1 数据层（2026-09-06 完成：27 店 / 135 菜 / 3 商圈演示数据）
- [x] S2 服务端 API（2026-09-06 完成：6 个 API + curl 全用例通过）
- [x] S3 前端骨架与转盘（2026-09-06 完成：Canvas 转盘 + 动画音效，rAF 兜底修复）
- [x] S4 店铺与下单（2026-09-06 完成：列表/详情/购物车/下单闭环）
- [x] S5 规则打磨与测试（2026-09-06 完成：排除上限/品类降级/偏好过滤/路由弹层修复；浏览器端到端走查通过）

> 实际执行说明：本次由 AI 按同一阶段顺序一次性完成，过程中发现并修复 3 个问题——
> ① 端口冲突（本机 3000 被占用，改用 8787）；② 转盘动画在 rAF 不可用环境卡死（增加 setTimeout 兜底）；
> ③ 品类转盘落点未套用偏好过滤（品类扇区改为只从偏好过滤后仍有店的品类生成）+ 路由切换未关闭弹层。

## 八、真实数据接入（V1.1，2026-09-06 追加）
- [x] 高德 Web 服务适配层 `server/lib/amap.js`（周边 POI / 关键字搜索 / 逆地理编码，Key 仅存服务端）
- [x] shopService 双数据源：`AMAP_KEY` 存在时实时拉取真实餐饮 POI（网格缓存 10 分钟、分页去重、id 注册表），否则回退演示数据
- [x] WGS-84 → GCJ-02 坐标系转换（修复国内定位与 POI 混算的数百米偏差；开发中发现偏心率常数写反导致 NaN，已修复）
- [x] 地址搜索 `/api/geocode/suggest` + 逆地理 `/api/geocode/reverse` 代理路由
- [x] POI 模式交易守卫：店铺页展示"复制店名 + 美团/饿了么"面板，服务端拒绝站内下单（POI_NO_ORDER）
- [x] 前端模式感知：首页规则文案、店铺列表/结果卡片缺失字段占位、我的页忌口区隐藏、地址搜索入口
- [x] 本地 mock 高德服务 `server/test/mock-amap.js`，无 Key 亦可端到端联调（含 key 无效失败路径）
- [ ] 后续：CPS 联盟链接（美团联盟 / 淘宝联盟-饿了么）、商家入驻与自有菜单库

## 九、商业化基建：CPS 归因与转化漏斗（V1.2，2026-09-09 追加）
- [x] CPS 跳转服务 `cpsService`：联盟模板 + 推广位归因走环境变量（CPS_SOURCE_ID / CPS_*_TEMPLATE），未配置回退平台官方入口
- [x] 归因跳转路由 `/api/cps/go`：服务端 302 记录点击（平台/店铺/来源），杜绝前端丢参
- [x] 全链路埋点 `lib/track.js` + `/api/track`：事件白名单（session/page/转动/出结果/点下单/平台跳转/站内下单/排除/拉黑/分享/复制），JSONL 持久化重启不丢
- [x] 漏斗看板 `/api/stats` + 「我的」页实时漏斗卡片（今日/累计/平台分布）
- [x] POI 模式结果卡片主按钮升级为"去美团外卖下单"CPS 链接，副链接提供饿了么与复制店名
- [x] 验证：302 Location 归因参数注入、事件白名单、重启数据恢复、demo/amap 双模式浏览器端到端
- [ ] 后续：真实联盟账号接入与佣金回流对账、团队拼单转盘（B 端前置）、分享卡片带邀请参数
